package com.example.demo.entities;

// import javax.persistence.*;
// Don't Know If Needed But Included Anyways
import jakarta.persistence.*;

public enum StatusType {
    pending, ordered, canceled ;
}
